{
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vite",
    "dev:web": "vite",
    "dev:api": "npm --prefix server run dev",
    "start:api": "npm --prefix server run start",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "node scripts/predeploy.mjs",
    "backup:db": "node scripts/backup-db.mjs",
    "db:deploy": "npm --prefix server run prisma:deploy",
    "seed:keywords": "npm --prefix server run seed:keywords",
    "db:seed": "npm --prefix server run db:seed",
    "prisma:generate": "npm --prefix server run prisma:generate"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
